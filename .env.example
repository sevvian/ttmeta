# --- Application Settings ---
# If API_KEY is set, requests must include the 'x-api-key' header with this value.
# If left empty, authentication is disabled.
API_KEY=

# --- CORS Settings ---
# Comma-separated list of allowed origins. Use "*" for all.
# Example: CORS_ORIGINS="http://localhost:8000,http://127.0.0.1:8000"
CORS_ORIGINS="*"

# --- LLM Settings ---
LLM_ENABLED=true
LLM_MODEL_PATH=/models/unsloth-qwen3-0.6b.gguf

# --- Performance Tuning ---
# Number of threads for llama.cpp. Should ideally match physical core count.
# The N5105 has 4 cores, so 2 or 4 is a good starting point.
OMP_NUM_THREADS=4
